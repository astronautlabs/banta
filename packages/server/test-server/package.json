{
  "name": "@banta/example-server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "buildx": "tsc -b",
    "start:win": "cd .. && npm run fix:firebase:win && cd test-server && npm run buildx && node dist/main",
    "start:mac": "cd .. && npm run fix:firebase:mac && cd test-server && npm run buildx && node dist/main"
  },
  "author": "Astronaut Labs, LLC",
  "license": "MIT"
}
