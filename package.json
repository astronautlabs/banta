{
  "name": "@engagechat/server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "npm run build && node dist/main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@alterior/runtime": "^3.0.0-beta.55",
    "@alterior/web-server": "^3.0.0-beta.59",
    "firebase-admin": "^8.8.0",
    "zone.js": "^0.10.2"
  },
  "devDependencies": {
    "typescript": "^3.7.2"
  }
}
